# relay-console

The Relay management console — a Next.js App Router app (SSR) for the **onboarding → build → operate**
flow. Runs on `:3100`; talks to the gateway control plane on `:3000`. See ADR `0011`.

## Layout

```
app/
├── layout.tsx            root shell (imports globals.css)
├── page.tsx              landing / Logto sign-in → links into the console
├── (console)/           authenticated area (shared nav shell)
│   ├── layout.tsx        gates on requireUser(); nav grouped build → operate → platform
│   ├── dashboard/        spend/usage tiles + setup checklist + spend-by-model
│   │   ── build ──
│   ├── apps/             applications list/create + [appId] keys (create/rotate/revoke)
│   ├── providers/        credential list (status + routing health) + [id] detail & dependents
│   ├── routes/           route editor: versions, targets, activate/rollback, cache toggle
│   ├── models/           /v1/models catalogue, joined to the org's routes ("routable?")
│   ├── playground/       real /v1/chat/completions call + x-relay-* inspector
│   │   ── operate ──
│   ├── analytics/        grouped usage report (model/app/route/day), window, CSV export
│   ├── traffic/          seeded history + live SSE feed, status filter, [requestId] trace
│   └── audit/            hash-chained audit trail, keyset-paginated
├── orgs/                 platform-admin org lifecycle, members + entitlements
├── platform/             cross-org spend, window + CSV export
├── api/                  route handlers: SSE proxy, CSV export, playground proxy
└── lib/
    ├── api.ts            typed client — control plane (Logto bearer) + data plane (virtual key)
    ├── api-types.ts      GENERATED from OpenAPI (`make generate`) — do not edit
    ├── auth.ts           requireUser / requireOrg / requireAdmin (server-side gates)
    ├── usage.ts          dashboard aggregation (pure, tested)
    ├── analytics.ts      grouping + reporting-window logic (pure, tested)
    ├── models.ts         catalogue grouping + route join (pure, tested)
    ├── providers.ts      credential↔route usage + health buckets (pure, tested)
    ├── traffic.ts        status filter + live-event merge (pure, tested)
    ├── playground.ts     completion/header response parsing (pure, tested)
    ├── onboarding.ts     org onboarding state machine (pure, tested)
    ├── checklist.ts      setup-checklist derivation (pure, tested)
    └── snippet.ts        cURL / Python / Node snippet builder (pure, tested)
components/
├── ui/                   shadcn primitives (button/card/input/label/table/dialog/badge/
│                         textarea/segmented-nav/feature-card)
└── *.tsx                 feature components (forms, dialogs, snippet drawer, playground, nav)
test/e2e/                 Playwright specs (gating = no auth; build-flow = seeded session)
```

## The two planes

The console talks to **both** of the gateway's surfaces, and they authenticate differently:

| Plane     | Endpoints              | Credential                                                                                      |
| --------- | ---------------------- | ----------------------------------------------------------------------------------------------- |
| Control   | `/api/v1/*`            | the caller's Logto access token, attached server-side by `lib/api.ts`                           |
| Data      | `/v1/chat/completions` | a **virtual key**, which the console never holds — the playground user supplies one per request |
| Discovery | `/v1/models`           | none (unauthenticated by design)                                                                |

Anything needing a browser-initiated call (SSE, CSV download, the playground) goes through a
same-origin route handler under `app/api/`, so credentials stay server-side and the gateway needs no
CORS allowance for the console.

## Postman

`api/postman/` holds a generated collection covering every endpoint — see
[`api/postman/README.md`](../../api/postman/README.md). It is regenerated by `make generate`.

## Conventions

- **Auth is server-side.** Gate every protected page with `requireUser`/`requireOrg`/`requireAdmin`;
  never rely on hiding UI. The gateway still enforces scopes on every call.
- **All data via the typed client.** `api-types.ts` is generated — after any endpoint change run
  `make generate` and commit `openapi.json` + `api-types.ts` together.
- **Mutations are server actions** that call `api.ts` and `revalidatePath`.
- **One-time keys / write-only secrets:** reveal a key's plaintext once (copy + snippet); provider
  secret fields are password inputs and are never read back.
- **Pure logic lives in `lib/*.ts`** and is unit-tested; components stay thin.

## Scripts

```bash
pnpm dev         # next dev on :3100
pnpm build       # production build (also type-checks routes)
pnpm test        # vitest unit tests (pure lib)
pnpm e2e         # playwright (needs the stack up: make dev)
pnpm gen:api     # regenerate api-types.ts from the OpenAPI spec
```
